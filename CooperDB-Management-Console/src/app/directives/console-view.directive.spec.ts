import { ConsoleViewDirective } from './console-view.directive';

describe('ConsoleViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ConsoleViewDirective();
    expect(directive).toBeTruthy();
  });
});
